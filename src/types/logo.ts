import { CSSProperties } from "react";

export interface LogoIconProps {
  size?: number;
  color?: string;
  style?: CSSProperties;
  className?: string;
  active?: boolean;
  path?: string;
  activePath?: string;
}
